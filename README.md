### Requirements
#### User Journey
- user visits site
- user can login/signup 
- user pastes url into text bar and click "shorten URL" button
- shortened url is shown with "copy" button
- user scrolls down to stats to see how many clicks per time period their link gets